model_type: controller
num_classes: 100
data_dir: /home/arooba/compute-aware-vit-thesis/data/
output_dir: checkpoints/controller_clean_split
batch_size: 32
epochs: 20
learning_rate: 0.0001
weight_decay: 0.0001
seed: 42
aux_loss_weight: 0.5
# Threshold pairs are evaluated post-training in run_controller_eval:
# (0.9,0.7), (0.8,0.6), (0.7,0.5), (0.6,0.4), (0.5,0.3)
pretrained: true
# Clean train/val/test protocol (val held out from official train split)
split_seed: 42
val_size: 5000   # carved from the 50k official train split -> 45k train / 5k val
